fastapi==0.104.1
uvicorn==0.24.0
python-dotenv==1.0.0
# redis>=4.2 ships built-in async (redis.asyncio) — aioredis is deprecated & conflicts
redis==5.0.1
httpx==0.25.0
pydantic==2.4.2
pydantic-settings==2.0.3
sentence-transformers==2.2.2
scikit-learn==1.3.2
apscheduler==3.10.4
groq==0.4.2
psycopg2-binary==2.9.9
sqlalchemy==2.0.23
numpy>=1.24.0
asyncpg==0.29.0            # Tier 4.1 — async PostgreSQL driver for SQLAlchemy

# Tier 3 additions
vaderSentiment==3.3.2          # 3.1 — lightweight NLP sentiment (no C++ needed, ~2MB)
pytrends==4.9.2                # 3.5 — Google Trends overlay (unofficial API, cache aggressively)
python-jose[cryptography]==3.3.0  # 3.4 — JWT auth tokens
passlib[bcrypt]==1.7.4         # 3.4 — password hashing
websockets==12.0
urllib3<2.0.0

